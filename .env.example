# ==============================================================================
# PLUMBER AGENTS - ENVIRONMENT CONFIGURATION
# ==============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit the .env file to git (it's in .gitignore)

# ------------------------------------------------------------------------------
# COMMUNICATION MODE
# ------------------------------------------------------------------------------
# Options: 'cli', 'whatsapp', 'auto'
# - cli: Debug mode using terminal stdin/stdout
# - whatsapp: Production mode using WhatsApp Cloud API
# - auto: Automatically detect (uses WhatsApp if credentials available, else CLI)
COMMUNICATION_MODE=cli

# ------------------------------------------------------------------------------
# WHATSAPP CLOUD API CREDENTIALS (Meta/Facebook)
# ------------------------------------------------------------------------------
# Required only if COMMUNICATION_MODE=whatsapp
# Get these from: https://developers.facebook.com/apps/

# Your permanent access token from Meta App Dashboard
# Path: App Dashboard > WhatsApp > API Setup > Temporary access token
# NOTE: Generate a permanent token for production use
WHATSAPP_ACCESS_TOKEN=your_access_token_here

# Your WhatsApp Phone Number ID
# Path: App Dashboard > WhatsApp > API Setup > Phone Number ID
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id_here

# Your WhatsApp Business Account ID (optional)
# Path: App Dashboard > WhatsApp > API Setup > WhatsApp Business Account ID
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id_here

# App Secret for webhook signature verification (security)
# Path: App Dashboard > Settings > Basic > App Secret
WHATSAPP_APP_SECRET=your_app_secret_here

# Webhook verification token (you choose this value)
# Must match the value you enter in Meta App Dashboard when setting up webhook
WHATSAPP_VERIFY_TOKEN=plumber_agents_verify_token_12345

# ------------------------------------------------------------------------------
# CONTACT INFORMATION
# ------------------------------------------------------------------------------
# Phone numbers for WhatsApp mode (format: country code + number, no + or spaces)
# Examples: 1234567890 (US), 491234567890 (Germany)
# For CLI mode, these are just display names

# Field technician's phone number or name
FIELD_TECHNICIAN_PHONE=Frank

# Office staff's phone number or name
OFFICE_STAFF_PHONE=Office Staff

# ------------------------------------------------------------------------------
# GEMINI API (for LLM agents)
# ------------------------------------------------------------------------------
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ------------------------------------------------------------------------------
# WEBHOOK SERVER CONFIGURATION
# ------------------------------------------------------------------------------
# Port for webhook server (default: 8010)
# The webhook server receives incoming WhatsApp messages
WEBHOOK_PORT=8010

# Office agent API port (default: 8001)
# Used for field agent -> office agent communication
OFFICE_AGENT_PORT=8001

# ------------------------------------------------------------------------------
# PRODUCTION DEPLOYMENT (Optional)
# ------------------------------------------------------------------------------
# Public URL where your webhook server is accessible
# Required for WhatsApp webhook setup in Meta Dashboard
# Example: https://your-domain.com or https://your-ngrok-url.ngrok.io
WEBHOOK_PUBLIC_URL=https://your-domain.com
